# v1.12 → v2.0 migration guide

Issue [#527](https://github.com/ayutaz/piper-plus/issues/527) の Python 3.13 + CUDA 12.8 + Ubuntu 24.04 統一に伴う **breaking change の対応手順**。 v1.12 から v2.0 にアップグレードするユーザー向け。 本リリースは Zero-Shot TTS ([PR #222](https://github.com/ayutaz/piper-plus/pull/222)) と統合した major リリース v2.0.0 として publish される。

設計詳細は [`docs/reference/python-313/`](../reference/python-313/) の 5 文書 (要求定義 / 要件定義 / マイルストーン / 未決事項 / 実装計画) を参照。

---

## Docker base image upgrade

### 影響

- `docker/python-train/Dockerfile` と `docker/python-inference/Dockerfile` の base image が:
  - `nvidia/cuda:12.6.3-cudnn-*-ubuntu22.04` → `nvidia/cuda:12.8.1-cudnn-*-ubuntu24.04`
  - Ubuntu 22.04 (Jammy、 EOL 2027-04) → Ubuntu 24.04 (Noble、 EOL 2029-04)
  - CUDA 12.6.3 → CUDA 12.8.1 (Blackwell sm_120 正式対応)

### 必要な対応

**host 側 (Docker 実行環境):**

1. **NVIDIA driver を R570 以上にアップグレード**:

   ```sh
   nvidia-smi  # "Driver Version: 570.x" 以上であることを確認
   ```

   R525 (CUDA 12.6 相当) 以下の host では新 image が起動失敗します。 driver bump はサーバー管理者に依頼してください。

2. **`nvidia-container-toolkit` を 1.14 以上にアップグレード**:

   ```sh
   nvidia-container-cli --version  # 1.14+ を確認
   ```

   1.10 以下では CUDA 12.8 image を `--gpus all` で実行する際に device mount が失敗するケースがあります。

3. **`docker run` で `--gpus all` が動くことを確認**:

   ```sh
   docker run --rm --gpus all nvidia/cuda:12.8.1-base-ubuntu24.04 nvidia-smi
   ```

### Fallback (v1.12 image を継続利用)

driver を上げられない場合は **v1.12 Docker image tag を継続利用** してください。 v1.12 系の tag は registry で長期保持されます (Issue #527 / DR-006 / OQ-14 で確定)。

```sh
# 旧 image 利用例
docker pull <registry>/piper-plus-gpu:1.12.0
docker pull <registry>/piper-plus-cpu:1.12.0
```

---

## Python 3.13 default

### 影響

- Docker 全 image の Python interpreter が 3.11 → 3.13 に統一
  - CPU image: `python:3.11-slim-bookworm` → `python:3.13-slim-trixie`
  - distroless: `distroless/python3-debian12` (= 3.11) → `distroless/python3-debian13` (= 3.13)
  - CUDA image: apt `python3.11` → deadsnakes PPA `python3.13`
- CI workflow 20 個の default `python-version` が `'3.11'` → `'3.13'` に
- ドキュメント (`CONTRIBUTING.md` / `README*.md` 等) の Python 推奨 version が 3.11+ → 3.13+ に

### 互換性

**`requires-python = ">=3.11"` は据置** (Issue #527 / NFR-01 / C-01)。 PyPI 経由の `pip install piper-plus` は Python 3.11 / 3.12 / 3.13 すべてで動作継続します。

### 必要な対応 (3rd party 利用者)

- Docker image を使う場合: host driver bump (上記 [Docker base image upgrade](#docker-base-image-upgrade)) のみ
- PyPI 経由: 対応不要 (3.11/3.12 サポート維持)
- ローカル開発環境: Python 3.13 への切替を推奨 (`uv python install 3.13`)

---

## PyTorch upgrade

### 影響

- `docker/python-train/Dockerfile` の torch wheel が:
  - `torch==2.2.1+cu121` → `torch==2.11.0+cu128`
  - `torchaudio==2.2.1+cu121` → `torchaudio==2.11.0+cu128`
  - **`torchvision==0.17.1+cu121` は削除** (piper-train が未使用、 FR-02-05)
- `pyproject.toml` の floor pin: `torch>=2.11.0` (DR-002 と整合)

### 互換性

**新 GPU 起動の必須条件** (Issue #527 / DR-001):

- RTX 5090 (Blackwell sm_120) は CUDA 12.8 wheel (cu128) が必須、 cu121 wheel では起動不可
- Ada 6000 (sm_89) / T4 (sm_75) は cu121 でも動くが、 cu128 で Tensor Core / cuDNN 9.5+ の恩恵

### 必要な対応

ローカルで torch を直接 install している場合:

```sh
# 新環境
uv pip install --index-url https://download.pytorch.org/whl/cu128 \
    "torch==2.11.0+cu128" "torchaudio==2.11.0+cu128"
```

---

## Windows local dev (cross-platform notes)

学習は Linux 想定だが、 ローカル dev / 推論検証は Windows でも可能。 v2.0.0 (2026-05-25 release) には Windows 罠が残っていたが、 PR #581 (v2.0.1 予定) で 2 つの罠を透過対応した。

### Issue 1: Windows + GPU で自動的に cu128 wheel が入る (修正済)

PR #581 で `pyproject.toml` の cu128 source marker を `sys_platform == 'linux'` から `sys_platform != 'darwin'` に拡張したため、 `uv sync` だけで Windows + GPU 環境にも `torch 2.11.0+cu128` が install される。 macOS のみ CUDA 非対応のため除外 (CPU wheel が入る)。 手動の `uv pip install --index-url ...` は不要。

> `onnxruntime` も同様に Windows + GPU では `onnxruntime-gpu` を別途 install 必要 (これは piper-plus 側の修正対象外、 ORT 配布ポリシーの制約)。

<details>
<summary>Pre-v2.0 workaround (no longer needed)</summary>

v1.12 以前は cu128 index marker が `sys_platform == 'linux'` 限定だったため、 Windows では CPU-only wheel が install され、 以下の手動コマンドで上書きが必要だった:

```powershell
uv pip install --index-url https://download.pytorch.org/whl/cu128 `
    "torch==2.11.0+cu128" "torchaudio==2.11.0+cu128"
```

</details>

### Issue 2: Linux で保存した ckpt を Windows で `torch.load` (透過対応済)

PR #581 で `piper_train` を import すると `piper_train._compat` が eagerly loaded されるようにした。 Windows では `pathlib.PosixPath` が `pathlib.WindowsPath` に透過的に置換される。 `torch.load(ckpt, weights_only=False)` が Windows でも Linux 製 ckpt をそのまま読める (`piper_train.infer` / `piper_train.export_torchscript` / `piper_train.clean_cached_audio` 等の全 entry で有効)。

背景: Linux で `pickle` シリアライズされた ckpt 内には `pathlib.PosixPath` インスタンスが含まれる (Lightning が hparams / callback で path を保存する)。 Windows で load しようとすると以下のエラーが出る (torch 2.11 固有ではなく、 torch 2.2 / 2.10 でも同じ universal な互換問題):

```text
pathlib._abc.UnsupportedOperation: cannot instantiate 'PosixPath' on your system
```

`piper_train._compat` がこれを抑止する。 ただし ckpt 内の path 文字列は Windows filesystem 上では当然存在しないため、 重み参照 (`state_dict` 読み出し) は問題ないが、 **実 resume training を Windows で続行する場合は path を rewrite する必要あり** (通常はそうではなく、 Windows は inspect 用、 学習は Linux に戻すワークフローを想定)。

<details>
<summary>Pre-v2.0 manual workaround (no longer needed)</summary>

v1.12 以前は load 前にユーザー側でモンキーパッチが必要だった:

```python
import pathlib
pathlib.PosixPath = pathlib.WindowsPath  # または PurePosixPath
import torch
ckpt = torch.load("epoch=32-step=216326.ckpt", map_location="cpu", weights_only=False)
```

</details>

---

## Checkpoint resume non-support

### 影響 (重要)

**v2.0 では torch 2.2 系で生成された旧 ckpt の resume を保証しません** (Issue #527 / DR-006):

- `--resume-from-multispeaker-checkpoint /data/piper/output-multilingual-6lang/.../*.ckpt` (torch 2.2 製) は **未サポート**
- PyTorch upstream の互換ポリシー: model weights は forward 互換とされるが、 optimizer state_dict と internal key 構造に変更があり、 piper-train で resume 失敗する可能性
- 既存 ONNX モデル (HuggingFace 配布の `piper-plus-base` 等) の **推論は forward 互換で動作継続**

### ✅ 部分的に検証済み (2026-06-20、PR #222)

torch 2.2 製の v7 ckpt (`epoch=32-step=216326.ckpt`、947MB) を **Python 3.13 + torch 2.11 + onnxruntime 1.26 (Windows、`torch 2.11.0+cpu`)** にて `torch.load(weights_only=False)` した結果:

| 項目 | 結果 |
|---|---|
| state_dict deserialize (816 tensors) | ✅ OK |
| zero-shot 重み (`spk_proj` 192→512 MLP / `dino_center` / `spk_proj_teacher`) | ✅ shape 一致 |
| `optimizer_states` (2 optimizer、686+111 params) | ✅ OK |
| `lr_schedulers` (2 cosine warmup) | ✅ OK |
| EMA states (3 callback、`decay=0.9995`, `num_updates=109527`) | ✅ OK |

つまり **file deserialization は torch 2.11 で問題なく通る**。 DR-006 で「保証しない」と書いた `--resume_from_checkpoint` 後の実 1 step 学習は本検証では未確認のため引き続き no-guarantee 扱い (Lightning が optimizer state を完全に restore できるかは GPU + dataset での実測が必要)。

### 必要な対応

#### 推論ユーザー (大多数)

**対応不要**。 既存 ONNX モデルは v2.0 ランタイム / Docker image で動作します。

#### 学習ユーザーで旧 ckpt から継続学習が必要な場合

選択肢:

**A. v1.12 Docker image で継続学習** (推奨):

```sh
# v1.12 image を使い続ける
docker run --gpus all <registry>/piper-plus-gpu:1.12.0 \
    python -m piper_train --resume-from-multispeaker-checkpoint <old_ckpt> ...
```

v1.12 image は registry で長期保持されます (OQ-14)。

**B. v2.0 で from scratch 再学習**:

```sh
# 新規 base 学習 (Template A、 75 epoch 規模)
python -m piper_train \
    --dataset-dir <DATASET_DIR> \
    --accelerator gpu --devices 4 --precision bf16-mixed \
    --max_epochs 75 --batch-size 32 \
    ...
```

**C. v2.0 で torch 2.11 製 base ckpt から FT** (M5 リリース後に新 base を公開予定):

公開後は `--resume-from-multispeaker-checkpoint <new_base.ckpt>` で v2.0 環境 FT 可能。

---

## distroless image upgrade

### 影響

- `docker/python-inference/Dockerfile.cpu.distroless` と `docker/webui/Dockerfile.distroless` で:
  - builder: `python:3.11-slim-{bookworm,trixie}` → `python:3.13-slim-trixie`
  - final: `gcr.io/distroless/python3-debian12` → `gcr.io/distroless/python3-debian13`
  - 内部パス: `/usr/local/lib/python3.11/site-packages` → `/usr/local/lib/python3.13/site-packages`
  - `PYTHONPATH` 環境変数も同様に書換

### 必要な対応 (3rd party がこれらを extends している場合)

`FROM <piper-plus-distroless>:1.12.0` を使っていてカスタム overlay を追加している場合、 内部パス参照を 3.13 に追従:

```dockerfile
# Before (v1.12)
COPY my-overlay/ /usr/local/lib/python3.11/site-packages/

# After (v2.0)
COPY my-overlay/ /usr/local/lib/python3.13/site-packages/
```

---

## TF32 default ON

### 影響

`src/python/piper_train/__main__.py` の `main()` で:

```python
torch.backends.cudnn.benchmark = True
# v2.0 で追加 (DR-007)
torch.backends.cuda.matmul.allow_tf32 = True
torch.backends.cudnn.allow_tf32 = True
```

- Ada/Blackwell (sm_80+): TF32 Tensor Core で matmul/conv ~1.3-1.5x 高速化
- T4 (sm_75) 以下: noop (warning なし)
- 数値精度: matmul mantissa 23-bit → 10-bit、 perceptual 影響は audio_parity Tier 4 (SNR ≥ 30dB) で検証済

### 必要な対応

通常は対応不要。

`torch.use_deterministic_algorithms(True)` を併用する research 用途は TF32 と非互換のため、 必要に応じて以下を `__main__.py` 修正で OFF にしてください (将来的に opt-out CLI flag 追加検討):

```python
torch.backends.cuda.matmul.allow_tf32 = False
torch.backends.cudnn.allow_tf32 = False
```

---

## bf16-mixed Template default

### 影響

`CLAUDE.md` の Template A (multi-speaker pretraining) と Template B (single-speaker FT) の `--precision` default:

- Before: `--precision 32-true` (V100 想定、 V100 で FP16-mixed は backward 遅延あり)
- After: `--precision bf16-mixed` (Ada/Blackwell で BF16 native Tensor Core 活用)

加えて `--no-wavlm` も Template から削除 (Ada/Blackwell で WavLM 有効が canonical):

```diff
- --accelerator gpu --devices 1 --precision 32-true \
+ --accelerator gpu --devices 1 --precision bf16-mixed \
- --ema-decay 0.9995 --max-phoneme-ids 400 --no-wavlm \
+ --ema-decay 0.9995 --max-phoneme-ids 400 \
```

### 必要な対応

#### 旧 V100 環境を継続利用する場合

`--precision 32-true` を明示指定:

```sh
python -m piper_train \
    --accelerator gpu --devices 4 --precision 32-true \
    --no-wavlm \
    ...
```

#### T4 (VRAM 16GB) で学習する場合

T4 は学習向きではない (VRAM 制約) が、 やる場合は:

```sh
--precision 16-mixed \  # T4 は sm_75 で BF16 native 非対応
--no-wavlm \            # VRAM 節約
--batch-size 8 \        # 16GB 上限内
```

#### Ada 6000 / RTX 5090 で学習する場合

Template の default をそのまま使ってください:

```sh
--precision bf16-mixed \
--batch-size 32 \  # Ada 48GB / RTX 5090 32GB で快適
```

---

## Affected packages (DR-005)

v2.0.0 リリースで影響を受けるパッケージ一覧。 各ランタイムは independent versioning のため、 piper-plus (Python) のみが 2.0.0 bump 対象。 他ランタイムは個別タイミングで bump される。

| パッケージ | レジストリ | 旧 version | 新 version | bump 種別 |
|-----------|----------|-----------|----------|----------|
| `piper-plus` (Python) | PyPI | 1.12.0 | **2.0.0** | major bump (Issue #527 + PR #222 統合リリース) |
| `piper-plus` / `piper-plus-cli` (Rust) | crates.io | 0.4.0 | 0.4.0 (M5 以降に判断) | independent — DR-001〜008 と直接連動しない |
| `PiperPlus.Core` / `PiperPlus.Cli` (C#) | NuGet | 0.3.0 | 0.3.0 (M5 以降に判断) | independent — 学習側変更のため runtime API 影響なし |
| `piper-plus` (npm) | npm | 0.6.0 | 0.6.0 (M5 以降に判断) | independent |
| `PiperPlus` (Swift xcframework + SPM) | git tag | 1.13.0 | 1.13.0 | bump 同期予定 (M5) |
| `piper-plus-g2p-android` (Kotlin) | Maven Central | 1.0.x | 1.0.x (M5 以降に判断) | independent |
| `libpiper_plus` (C API) | shared lib | — | — | tag-based、 リリースアセット連動 |

> **解釈**: Issue #527 単体は学習側 (Docker / pyproject.toml / training pipeline) の刷新で runtime API を破壊しないため SemVer 上は minor 相当だが、 Zero-Shot TTS ([PR #222](https://github.com/ayutaz/piper-plus/pull/222)) と統合した major リリースとして **v2.0.0** で publish する。 そのため Python を 2.0.0 bump、 他ランタイムは個別の next release で bump される。 詳細は [milestones.md](../reference/python-313/milestones.md)。

---

## Post-merge verification checklist (DR-009)

リリース前に実機環境で実施する smoke verification。 各項目に責任者と完了日を記録する。

### GPU 環境別 smoke test

| 環境 | 検証項目 | 期待結果 | 完了日 |
|------|---------|---------|-------|
| **Ada 6000 (48GB, sm_89)** | Template B FT 50 epoch (`--precision bf16-mixed`) | loss 推移正常、 OOM なし、 TF32 default ON | — |
| **RTX 5090 (32GB, Blackwell sm_120)** | torch 2.11+cu128 で Template B 起動 + 5 epoch | sm_120 認識、 学習 step 成立 | — |
| **T4 (16GB, sm_75)** | Template B (`--precision 16-mixed`, `--no-wavlm`) で 5 epoch | 16-mixed で起動 (sm_75 は BF16 native 非対応、 本ガイド「T4 で学習する場合」と整合)、 VRAM 制約内 | — |

### ビルド検証

- [ ] Docker build (`docker/python-inference/Dockerfile`): `nvidia/cuda:12.8.1-cudnn-runtime-ubuntu24.04` ベースで成功
- [ ] Docker build (`docker/python-train/Dockerfile`): `nvidia/cuda:12.8.1-cudnn-devel-ubuntu24.04` (builder) ベースで成功
- [ ] Docker build (`docker/webui/Dockerfile`): `python:3.13.13-slim-trixie` (CPU 専用) ベースで成功
- [ ] Docker build (`docker/wyoming/Dockerfile`): `python:3.13.13-slim-trixie` (CPU 専用) ベースで成功
- [ ] pip install on Python 3.13 (Linux/macOS/Windows): `uv pip install piper-plus` が成功

### 推論検証

- [ ] CSS10 JA 6lang ONNX で推論 smoke (Python ランタイム)
- [ ] 全ランタイム (Rust/C#/Go/WASM/C++) の audio parity 一致確認
- [ ] WandB Audio Logging が新環境で動作

### 公開検証

- [ ] HF ckpt 公開: torch 2.11 製 6lang base ckpt を HuggingFace に push
- [ ] PyPI: `piper-plus 2.0.0` 公開、 install 成功
- [ ] CHANGELOG anchor の cross-link が render される

> **実施タイミング**: 環境準備完了次第 (RTX 5090 / Ada 6000 ハードウェア手配後)。 ブロッカーが発生した場合は v2.0.0 リリースを保留し、 [open-questions.md](../reference/python-313/open-questions.md) に新規 OQ として記録する。

---

## 関連リソース

- [Issue #527](https://github.com/ayutaz/piper-plus/issues/527) — GitHub Issue
- [`docs/reference/python-313/`](../reference/python-313/) — Issue #527 設計ドキュメント (5 文書)
  - [要求定義 (`requirements.md`)](../reference/python-313/requirements.md): WHAT/WHY + 互換性影響評価 (B-A1〜F3)
  - [要件定義 (`specifications.md`)](../reference/python-313/specifications.md): HOW MUCH + ADR (DR-001〜009)
  - [マイルストーン (`milestones.md`)](../reference/python-313/milestones.md): M1-M5 進捗管理
  - [未決事項 (`open-questions.md`)](../reference/python-313/open-questions.md): 全 14 OQ 決定済
  - [実装計画 (`README.md`)](../reference/python-313/README.md): Phase 別 diff + rollback
- 旧 image tag: registry 長期保持 (v1.12.x はサポート継続、 hotfix が必要なら v1.12 branch で対応)

---

文書作成日: 2026-05-25
バージョン: 1.0 (初版)
