# Zero-Shot v8 Handoff — 2026-07-09 セッション終了

## 追記: 2026-08-02 再開セッション

2026-08-02 に origin/dev (v2.0、 piper→piper_plus フル改名込) へのリベースが完了し、
branch 全 57 commit の hash が書き換わった (本文中の commit hash は新 hash に張替済、
テスト 170 件 green)。 以下、 本文 (2026-07-09 時点) からの主な差分:

- **KsponSpeech (AI-Hub、 research-only license) は v8 から除外確定**。 v8 モデルは
  過去の piper-plus 事前学習モデル同様 **public + 商用利用可の open-model として公開**
  する方針のため。 ko は **Zeroth-Korean (CC BY 4.0) + Common Voice ko
  (CC0、 fsicoli/common_voice_22_0 の ko subset)** のみで ~210 spk / ~25k utts。
  7-lang 合計: 話者 ~3,790 / 発話 ~346k。 data-sources.yml の kspon-speech
  エントリも削除済。
- **検証済み**: vast.ai 残高 $2,483.52 / HF token (gated moe-speech-plus read +
  private repo write、 ckpt 退避 repo `ayousanz/piper-plus-zero-shot-multi-6lang-v8`
  作成済) / WANDB キー / CV ko ソース (fsicoli/common_voice_22_0)。
  → HF トークン系の開始ブロッカーは解消。
- **新規追加**: `piper_train.tools.score_utmos` (commit `1eb2a3d0`) — SpeechMOS
  (tarepan/SpeechMOS、 utmos22_strong) で CV clip の per-clip UTMOS tsv を生成し
  `export_common_voice_ko --utmos-tsv` に渡す (これが無いと UTMOS フィルタは
  黙って無効化される)。 env prefix の v2.0 追従 `PIPER_EMB_*` → `PIPER_PLUS_EMB_*`
  (commit `e101b425`)。 moe-speech cache テストは `--no-parallel` で serial 経路に
  pin 済 (commit `878b6b14`)。
- **承認済み運用**: **4x A100 SXM4 40GB $3.43/hr (storage 1.7TB 込) を承認済**
  (2026-08-02 時点の筆頭 offer id=44347432。 オファーは流動的でレンタル時に再検索)。
  batch=32/GPU の VRAM は 40GB で未検証 → **smoke test を VRAM gate** とし、
  溢れたら batch=24 に下げるか 80GB ($7.68/hr) に乗換。
  **smoke 自動 go 基準** (承認済): sec/step ≤ 8.5 かつ Non-finite skip ≤ 3% かつ
  VRAM が 40GB に収まる → **報告を待たず本走 launch**。
  本走見積 (7-lang、 40GB 案): **7-9 日 / ~$600-750** (§3.8 の 7-8 sec/step 達成前提、
  10.74 のままなら +30%)。 本文の "$5.19/hr (80GB)" 系見積は市場変動で古い。
- **残 TODO (本走前)**: onstart 自動レジューム script / HF ckpt epoch ごと自動
  upload script (いずれも未作成)。 ko データパイプラインは実データ未検証
  (synthetic unit test のみ) → instance で小規模先行検証。 評価用話者 holdout は
  コード機構なし → dataset.jsonl 構築後に post-filter する運用
  (ja: moe-speech-plus 学習除外 ~10 名・男性含む / ko: Zeroth 学習除外 5-10 名)。

---

## 状態: **v8 + ko (7 lang) 学習 go 待ち** (2026-08-02 更新: smoke 自動 go 基準承認済 — 基準充足なら報告を待たず本走 launch、 上記追記参照)

このセッションで v8 の scratch NaN blocker を解消し、 5 施策で throughput -23% を実測。
続いて残 findings 8 施策 (T3 / T6 / T1-ext / T-empty / T-npy / P3 / P4 / P5)
をコード側で完遂 (design doc §3.8 参照)。 さらに **韓国語 (ko) 追加を data pipeline
のみで完遂** (parser 3 種 + CV ko exporter、 impl1 + impl2 の 2 段階、 §3.10 / §6.5)。
残りは 4x A100 DDP で **12-15 日 / ~$1,050-1,530 (7-lang 想定)** の本走を回すのみ
(→ 2026-08-02 訂正: KsponSpeech 除外 + 40GB $3.43/hr 承認後の見積は **7-9 日 / ~$600-750**、 冒頭追記参照)。

---

## 完了項目 (feature branch: `feat/zero-shot-v8-dataset-scaling`)

### コード修正 (合計 24 commits)

| commit | 内容 | カテゴリ |
|---|---|---|
| `168d22e2` | Super-MAS Triton kernel を docker/python-train に取り込み | 前準備 |
| `81f88817` | TF32 matmul precision + mel debug sync 削除 | P0 |
| `355f89eb` | MPD y/y_hat batch concat (12→6 kernel launch) | Plan A |
| `14847fe7` | DDPStrategy static_graph=True | Plan A |
| `dbdef2d2` | length_bucket opt-in (実測で逆効果、 default OFF) | Plan A |
| `15b33fd8` | torch.compile mode/dynamic CLI 化 | Plan A |
| `3dcabd57` | mel/spectrogram STFT を bf16→fp32 upcast (cuFFT bug) | **必須 fix** |
| `1a86aa55` | logs_p/logs_q を [-15,15] に clamp | KL 修正 v1 |
| `563f8361` | MAS 拡張後にも clamp | KL 修正 v1 |
| `a5fa6f2b` | SpeakerBalancedBatchSampler Fix B (length-binned) | 実験 |
| `1f625a42` | logs_p/m_p を [-8,8]/[-100,100] tighten + kl_loss cap 1e4 | **KL 修正 v2** ← 最終 |
| `3fba8275` | Batch.pin_memory() + prefetch_factor=4 | T2 (最大寄与) |
| `25c32636` | SDPA backend 明示制御 + logging | T5 |
| `597f589c` | channels_last opt-in on Conv2d Discriminator | T1 |
| `d5960b2a` | VAD/audio-cache workers 並列化 + tqdm | P2 |
| `51024040` | precompute_mel .npy tool | P1 |
| `acdcca36` | prepare_moe_speech_plus 並列化 (ProcessPool、 opt-in) | **P3** |
| `3ced31a8` | prepare_moe_speech_plus per-zip metadata cache | **P4** |
| `cc7c20a6` | CAM++ per-utt extraction default 化 (fixed_frames + chunk_batch) | **P5** |
| `e17d1f8e` | training_step の 500-batch empty_cache flush 除去 | **T-empty** |
| `7657a93c` | `--disc-precision` で D forward bf16 / SCL fp32 分離 | **T6** |
| `c0e4e0e3` | TextEncoder self-attention の SDPA fast path (opt-in `--attn-drop-rel-v`) | **T3** |
| `fc59900c` | MBiSTFTGenerator にも channels_last plumbing 対称化 | **T1-ext** |
| `e12d0be8` | audio_norm cache を .pt → .npy 切替 (write 側のみ、 backward-compat) | **T-npy** |
| `d6fc36a5` | mel_speaker_consistency_loss STFT を bf16→fp32 upcast + SDPA math backend 復活 | **T3/T6 bugfix** |
| `83fce90e` | §3.9 T3/T6 synthetic smoke A/B/C の結果を design doc に追記 | docs |
| `963c4c49` | v8 に韓国語 (ko) parser 3 種 + CV ko exporter 追加、 LANGUAGE_ID_MAP を 8-lang extended (ko=7) に拡張、 契約 language-id-map-contract.toml python_train を extended 版に切替 | **KO 追加** |
| `5b2f53c4` | **preproc impl1 (#1)**: `hf_transfer + HF_HUB_ENABLE_HF_TRANSFER=1` で HF Hub raw DL を 3-5x 高速化 (3-5h → 1-1.5h)、 train extras + Docker ENV + handoff § 2 export 手順 | **前処理 4 施策** |
| `c2f59ec0` | **preproc impl2 (#2)**: KsponSpeech `.pcm` (16 kHz mono s16le) を `norm_audio._read_pcm16_mono` で直読、 ETRI PCM→WAV pre-pass 3-4h を parallel VAD に融合 (~0) | **前処理 4 施策** |
| `e0fa0d04` | **preproc impl3 (#4)**: `export_libritts_r_from_parquet.py` / `export_cml_tts_from_parquet.py` を FLAC 直保存化 (magic 一致で `write_bytes` zero-copy、 downstream `soundfile` は FLAC/WAV 両受理)、 export 30-60min → 5-10min (2-6x) | **前処理 4 施策** |
| `b8539de4` | **preproc impl4 (#5)**: `prepare_moe_speech_plus.py --parallel` を `BooleanOptionalAction/default=True` に flip + `Pool.imap` chunksize 1→8、 選抜 30-45min → 3-5min (12-16x)、 CI は `--no-parallel` で opt-out | **前処理 4 施策** |

### 残 findings 8 施策の実装完了 (2026-07-09 追加、 design doc §3.8)

§3.7 で v8 config を確定した後、 review で未実装だった findings をコード側で完遂。
GPU 実測は次セッション (vast.ai instance destroy 済) だが、 各施策は単体テスト
40+ で契約 pin 済 + backward-compat は byte-for-byte 一致で保証。

**予測 wall-clock 更新** (§3.8 累積効果):

- 現行 sec/step: 10.74 (smoke2 baseline)
- 予測 sec/step: **7-8** (T-empty +2-3% × T-npy +5-10% × T3 +2-5% × T6 +5-10%、
  掛算ではなく overlap 保守見積)
- **単一 A100 SXM4**: 51 日 → **30-38 日** (最良 30 日)
- **4x A100 SXM4 DDP**: 9-11 日 → **6-8 日** (最良 6 日)
- **前処理 (P3/P4/P5)**: 8-10h → **2-4h**

詳細は [design doc §3.8](../design/zero-shot-v8-dataset-scaling-plan.md#38-残-findings-実装完了-2026-07-09-追加ランディング) 参照。

### 実測 (vast.ai A100 SXM4 80GB × 1)

| Test | 設定 | sec/step | Non-finite skip |
|---|---|---|---|
| Test 1 (baseline) | bf16-mixed + SCL + DINO + bucketing、 100 batches | 14.0 | 0/100 |
| **smoke2** ⭐ | + 5 施策 + KL v2、 300 batches | **10.74** | **6/300 (2%)** |
| smoke3b | + `--precomputed-mel` | 11.46 | 6/300 (2%) |

**Improvement**: **-23% sec/step** vs Test 1 (real config で)

### batch size 探索 (追加、 A100 SXM4 80GB × 1)

| batch | VRAM peak | utts/sec |
|---|---|---|
| **64** ⭐ | 40GB | **5.96** (最高) |
| 192 | 63GB | 5.82 |
| 224 | 73GB (91.6%) | 5.15 (-14%) |
| **256** | OOM | — |

→ **compute-bound、 batch 増加で throughput 悪化**。 **v8 本走は batch=32/GPU × 4x DDP (effective 128)** で単一 GPU の最良 utt throughput を維持しつつ並列化。

---

## v8 本走の推奨構成

**Plan B: 4x A100 SXM4 DDP、 batch=32/GPU (effective 128)**

見積: **9-11 日 / ~$1,120** (storage 込 $5.19/hr × 240 hr)

> **2026-08-02 訂正**: "$5.19/hr (80GB)" 見積は市場変動で古い。 承認済み構成は
> **4x A100 SXM4 40GB $3.43/hr (storage 1.7TB 込)**、 見積 **7-9 日 / ~$600-750**
> (§3.8 の 7-8 sec/step 達成前提)。 batch=32/GPU の VRAM は 40GB で未検証のため
> smoke を VRAM gate とし、 溢れたら batch=24 か 80GB ($7.68/hr) に乗換 (冒頭追記参照)。

CLI: [design doc §3.7 参照](../design/zero-shot-v8-dataset-scaling-plan.md#37-全-smoke-test-完了--最終見積-2026-07-09-セッション終了時点)

**除外フラグ (実測で不採用)**:
- `--compile`: torch inductor+triton kernel compile crash
- `--precomputed-mel`: GPU-bound で -6.5% 遅い
- `--enable-length-bucketing`: +34% 逆効果

---

## 学習開始手順 (次セッション用)

### 1. vast.ai instance 準備 (4x A100 SXM4 想定)

```bash
# search + rent
vastai search offers 'num_gpus=4 gpu_name=A100_SXM4 reliability>0.995 duration>21 disk>1700 inet_up>3'
vastai create instance <ID> --image nvidia/cuda:12.8.1-devel-ubuntu24.04 \
    --disk 1700 --label piper-v8-fullrun \
    --env "-e WANDB_API_KEY=... -e HF_TOKEN=..."
```

> **2026-08-02 追記**: 4x A100 SXM4 40GB $3.43/hr (storage 1.7TB 込、 筆頭 offer
> id=44347432、 disk 1874GB、 上下 4.3-4.5Gbps、 reliability 99.52%) を承認済。
> オファーは流動的なのでレンタル時に再検索する。 WANDB / HF token は検証済、
> vast.ai 残高 $2,483.52。

### 2. 環境セットアップ (30 分)

```bash
ssh root@<ip>
# torch 2.11 + venv + repo
cd /data && git clone https://github.com/ayutaz/piper-plus.git piper-plus
cd piper-plus && git checkout feat/zero-shot-v8-dataset-scaling
python -m venv /data/piper/.venv
/data/piper/.venv/bin/pip install torch==2.11.0+cu128 torchaudio==2.11.0+cu128 \
    --extra-index-url https://download.pytorch.org/whl/cu128
/data/piper/.venv/bin/pip install -e src/python/[train,super-mas]

# v8 前処理高速化 (#1): hf_transfer (Rust concurrent chunk downloader) を
# 有効化して HF Hub DL を 3-5x 高速化 (200-500MB/s、 単スレッド hf_hub の
# 40-80MB/s 比)。 hf_transfer は src/python[train] extras に同梱済みなので
# あとは env var を export するだけで大丈夫。 Docker image (docker/python-train)
# 側は Dockerfile に `ENV HF_HUB_ENABLE_HF_TRANSFER=1` 済み、 vast.ai bare-VM
# セットアップ時のみ明示 export が必要。
export HF_HUB_ENABLE_HF_TRANSFER=1
echo 'export HF_HUB_ENABLE_HF_TRANSFER=1' >> ~/.bashrc  # 再ログイン時も継続
```

> **有効性の確認**: `python -c "import hf_transfer; import os; print(os.environ['HF_HUB_ENABLE_HF_TRANSFER'])"`
> で `1` が返れば OK。 実 DL の中で `huggingface_hub` が Rust backend を
> 選択したかどうかは `HF_HUB_VERBOSITY=info hf download ...` の log で
> `Using hf_transfer` が出るかで判定できる (未 install なら fallback して
> RuntimeError にはならず単に旧 backend が使われるだけなので、 効いてる
> かどうかは必ず log か speed で確認する)。

### 3. データセット + CAM++ 再構築 (~7-10h、 ~$12-17)

**注意 (2026-07-09)**: vast.ai instance 44096702 は **削除済** (storage 課金停止のため)。
v8 dataset は破棄済みで、 次セッションでフル再構築が必要。

再構築手順 (推定時間、 前処理高速化 impl1-4 反映後 — [design doc §3.11](../design/zero-shot-v8-dataset-scaling-plan.md#311-前処理高速化-4-施策実装完了-2026-07-09-追加ランディング) 参照):

- 生データ DL (moe-speech-plus + LibriTTS-R + AISHELL-3 + CML-TTS + CV pt/ko + Zeroth + ~~KsponSpeech~~): **1-1.5h** (**impl1 hf_transfer** 有効時、 従来 3-5h から 3-5x 高速化、 §2 環境セットアップ参照)。 **2026-08-02: KsponSpeech は v8 から除外確定 (DL 不要)**
- `export_libritts_r_from_parquet.py` + `export_cml_tts_from_parquet.py` (impl3 FLAC 直保存): **10-20min** (旧 60-120min から double-decode 消去で 2-6x)
- `prepare_moe_speech_plus.py` (impl4 `--parallel` default ON + chunksize=8): **3-5min** (旧 30-45min から 12-16x)
- **KsponSpeech ETRI PCM→WAV 事前変換は不要** (impl2 で `norm_audio` が `.pcm` を直読、 従来必要だった 3-4h の pre-pass を skip、 §2.4 参照) — **2026-08-02: KsponSpeech 除外確定につき本項目自体が不要 (impl2 のコードは残置)**
- `prepare_multilingual_dataset.py` (P2 並列 VAD + `--ko-*` 引数): **~5-6h** (7-lang 版、 6-lang の場合 ~5h)
- `extract_speaker_embedding.py` (P5 fixed_frames + chunk_batch=128、 default 化済): **~30-45 min** (7-lang ~420k utts、 6-lang ~321k utts)
- `precompute_mel` (--precomputed-mel 使う場合、 実測で効果なしのため任意): 85 sec
- **合計 (6-lang)**: **~6-9h / ~$10-16**
- **合計 (7-lang、 ko 追加)**: **~7-10h / ~$12-17** (単一 A100 storage 込 $1.73/hr、 impl1-4 効果反映)。 2026-08-02: KsponSpeech 除外により ko は Zeroth + CV ko のみ (~25k utts) となり上限側は短縮方向

再構築が完了したら:
```
/data/piper/dataset-multilingual-6lang-v8/
├── dataset.jsonl              # 321,391 utts (期待値)
├── config.json                # num_speakers=3578, num_languages=6, num_symbols=185
├── speaker_embeddings/        # 321k × 192-dim CAM++ .npy
└── cache/                     # audio_norm .pt + spec .pt
/data/piper/models/campplus.onnx  # HF: ayousanz/campplus-onnx (private)
```

> **2026-08-02 追記**: 7-lang (ko 込、 KsponSpeech 除外) の期待値は
> dataset.jsonl **~346k utts** / **num_speakers ~3,790** / num_languages=7
> (ko は Zeroth ~181 spk / ~22.2k utts + CV ko ~30-50 spk / ~3k utts)。
> 出力先は **`/data/piper/dataset-multilingual-7lang-v8/`** とする (config.json の
> dataset label `multilingual-7lang` と揃えた命名、 design doc §3.7 の 2026-08-02
> 注記と同一)。

前処理 script の使用例は [`docs/handoff/zero-shot-tts-handoff-2026-06-20.md`](zero-shot-tts-handoff-2026-06-20.md) 参照。

### 4. 学習 launch (design doc §3.7 の CLI をそのまま)

```bash
export WANDB_API_KEY=$(grep WANDB_API_KEY /data/piper/.env | cut -d= -f2)
NCCL_DEBUG=WARN NCCL_P2P_DISABLE=1 NCCL_IB_DISABLE=1 \
    nohup /data/piper/.venv/bin/python -m piper_train [design-doc-§3.7-CLI] \
    > /data/piper/output-zero-shot-multi-6lang-v8-train.log 2>&1 &
```

> **2026-08-02 注記**: §3.7 の CLI は design doc 同節の 2026-08-02 注記に従って読み替える —
> `--dataset-dir` は 7-lang 構築後の **`/data/piper/dataset-multilingual-7lang-v8/`**、
> インスタンス/レートは 4x A100 SXM4 40GB $3.43/hr (本走 7-9 日 / ~$600-750、 VRAM は
> smoke gate)。 `--default_root_dir` / HF repo / log 名は `multi-6lang-v8` のまま維持。

### 5. 障害耐性

- checkpoint 2 epoch ごと (`--checkpoint-epochs 2 --save-top-k 5`)
- HF `piper-plus-zero-shot-multi-6lang-v8` に epoch ごと自動 upload (§4.4) — repo 作成済 ✅ (2026-08-02、 private)、 ただし **upload script は未作成** (本走前 TODO)
- instance 消失時は onstart script で自動レジューム — **script 未作成** (本走前 TODO、 2026-08-02 時点)

---

## 次のセッションで扱う候補

### 1. §3.8 施策の GPU smoke test (必須、 本走前)

vast.ai instance を新規に借りて以下 4 項目を 300 batch で確認 (§3.8 の予測を pin):

1. smoke2 baseline を rerun (10.74 sec/step 再現)
2. `--attn-drop-rel-v` + `--disc-precision bf16-mixed` を on にした累積 A/B (予測 8-9 sec/step)
3. audio_norm を `.npy` で pre-materialise (`ignore_cache=True` を短時間実行) → DataLoader load A/B
4. 合算で 7-8 sec/step 達成を確認、 未達なら nsys で bottleneck 再検証

> **2026-08-02 追記**: smoke は **VRAM gate も兼ねる** (batch=32/GPU が 40GB に
> 収まるか未検証 → 溢れたら batch=24 か 80GB 乗換)。 **自動 go 基準承認済**:
> sec/step ≤ 8.5 かつ Non-finite skip ≤ 3% かつ VRAM が 40GB に収まる →
> 報告を待たず本走 launch。 ko データパイプラインの実データ先行検証
> (synthetic unit test のみのため) もこの smoke で実施する。

### 2. v8 本走 (§3.8 実測後、 明示 go 待ち → 2026-08-02: smoke 自動 go 基準充足で launch に変更)

**Plan B 更新**: 4x A100 SXM4 DDP、 batch=32/GPU、 **6-8 日 / ~$800-1,100** (§3.8 予測反映後)

> **2026-08-02 更新**: 承認済み構成は 4x A100 SXM4 **40GB** $3.43/hr、 見積
> **7-9 日 / ~$600-750** (7-lang、 §3.8 の 7-8 sec/step 達成前提。 10.74 のままなら +30%)。

### 3. v9 検討事項 (v8 完走後)

- **Plan C: 8x A100 or H100 x 2** — 4-5 日で完走、 iteration さらに半減
- **`--compile` の torch 2.12/inductor 修正待ち** — kernel 対応で +10-25% 見込
- **v9: gol-dataset で 19,349 話者拡張** — v8 完走後の速度基盤の上で

---

## 追加ランディング (2026-07-09、 KO 拡張)

**同一 v8 run 内で ko=7 も学習** する副次拡張を feature branch にランディング:

- `LANGUAGE_ID_MAP` / `ALL_LANGUAGES` を 8-lang extended form に拡張
  (ja..pt indices は不変、 sv=6 / ko=7 が新規)
- `parse_zeroth_korean` / `parse_kspon_speech` / `parse_common_voice_ko` を
  `prepare_multilingual_dataset.py` に追加 (既存 aishell3 / cml_tts pattern と同一)
- `export_common_voice_ko.py` を追加 (`export_common_voice_pt.py` ミラー、
  UTMOS tsv 追加サポート)
- 契約 `language-id-map-contract.toml:python_train` を
  `extended_language_id_map` / `extended_languages` に切替
- data-sources.yml に Zeroth-Korean / KsponSpeech / Common Voice ko を追加
  (→ 2026-08-02: kspon-speech エントリはモデルカード生成への誤混入防止のため削除済)

**次セッションで必要な追加作業**:

- ~~KsponSpeech は raw `.pcm` (16 kHz mono s16le) を **そのまま** `--ko-ksponspeech`
  に渡す~~ (**impl2 [`c2f59ec0`](https://github.com/ayutaz/piper-plus/commit/c2f59ec0)** で
  `norm_audio._read_audio_any` が `.pcm` 拡張子を検出して直読、 従来必要だった
  ~3-4h の ETRI PCM→WAV 変換パスは削除。 既に WAV 化済の場合は
  `audio_ext=".wav"` を渡せば旧経路も維持)
  — **2026-08-02: KsponSpeech (AI-Hub research-only) は public + 商用可公開方針により
  v8 から除外確定。 本項目 (rsync / `.pcm` 受渡し手順含む) は不要**
- `python -m piper_train.tools.export_common_voice_ko --cv-dir ...` で CV ko 選別
  (~30 分、 UTMOS tsv を持っていれば同時に投入)
  — 2026-08-02: UTMOS tsv は **`piper_train.tools.score_utmos`** (commit `1eb2a3d0`) で生成して
  `--utmos-tsv` に渡す (無いと UTMOS フィルタは黙って無効化)。 フィルタ条件:
  UTMOS≥2.5 + ≥20clips + cap60 で ~30-50 spk / ~3k utts 見込
- `prepare_multilingual_dataset.py` に `--ko-zeroth` / ~~`--ko-ksponspeech`~~
  / `--ko-cv` を追加して再実行 (前処理 wall-clock 想定 +2h)
  — **2026-08-02: `--ko-ksponspeech` は使わない (KsponSpeech 除外確定)**。
  ko パイプラインは実データ未検証 (synthetic unit test のみ) のため
  instance で小規模先行検証してから本実行する
- 学習 CLI は変更不要 (dataset.jsonl に language_id=7 の utts が入り、
  emb_lang は config.json の `num_languages` から自動的に 7 次元化)
- 評価は §5 の SECS zero-shot に加えて **ko 未知話者の SECS 評価セット** を新設
  (Zeroth の学習除外 spk 5-10 名で計測)
  — ✅ 2026-08-02 運用確定: holdout のコード機構は作らず、 dataset.jsonl 構築後に
  post-filter する (ja: moe-speech-plus 学習除外 ~10 名・男性含む / ko: Zeroth
  学習除外 5-10 名)

詳細は [design doc §6.5](../design/zero-shot-v8-dataset-scaling-plan.md#65-korean-ko-追加--2026-07-09-追加ランディング) 参照。

---

## この記録の目的

- **次のセッションで v8 学習を open→run→報告まで実施できるようにする**
- 実測で判明した「使えないもの」を再検討しないため
- Plan A の楽観予測 (3-4 日) と実測 (9-11 日) の乖離理由を記録

## 関連ドキュメント

- [design doc §3.7](../design/zero-shot-v8-dataset-scaling-plan.md#37-全-smoke-test-完了--最終見積-2026-07-09-セッション終了時点) — 全 smoke 結果 + 最終 CLI
- [v7 handoff](zero-shot-tts-handoff-2026-06-20.md) — v7 学習環境の詳細
- [v7 training results](../design/multi-6lang-zero-shot-v7-training-results.md) — v7 完走時の実測 baseline
