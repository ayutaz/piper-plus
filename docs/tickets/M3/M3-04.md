# [M3-4] Rust/C#/Go/WASM ランタイムに Voice Cloning 統合

**マイルストーン**: M3a (v2.0.0)
**カテゴリ**: Voice Cloning
**工数**: 10d
**依存**: M3-3
**ステータス**: 未着手

---

## 1. 目的とゴール

M3-3 で学習した Voice Cloning モデル (VITS2 + speaker_embedding) を、Python / Rust / C# / Go / WASM の全5ランタイムで利用可能にする。参照音声から話者 embedding を抽出し、VITS 推論に注入する一連のパイプラインを各言語で実装する。

**ゴール:**
- 全5ランタイムで `synthesize(text, reference_audio)` 相当の API を提供
- Speaker Encoder ONNX の推論を各ランタイムで実装
- 音声前処理 (リサンプリング、メルスペクトログラム) を各ランタイムで統一仕様に準拠して実装
- 全ランタイムで同一参照音声 -> 同一 embedding (誤差 L2 < 1e-3) を保証

## 2. 実装内容の詳細

### 2.1 ランタイム別実装計画

| ランタイム | 工数 | 主要変更ファイル | 音声前処理 |
|-----------|------|-----------------|-----------|
| Python | 1.5d | `ort_utils.py`, `infer_onnx.py`, `api.py` | numpy + scipy |
| Rust | 2.5d | `engine.rs`, `phonemize/` | ndarray + rubato |
| C# | 2.5d | `Inference/`, `PiperPlus.Core/` | NAudio + custom mel |
| Go | 2d | `piperplus/`, `cmd/piper-plus/` | gonum + custom mel |
| WASM | 1.5d | `src/wasm/openjtalk-web/`, `piper-wasm/` | AudioContext API |

### 2.2 共通仕様 (docs/spec/speaker-encoder-preprocess.toml)

```toml
[audio]
sample_rate = 16000
n_fft = 400        # 25ms @ 16kHz
hop_length = 160   # 10ms @ 16kHz
n_mels = 80
fmin = 0
fmax = 8000
window = "hann"
normalize = true   # per-utterance mean/var normalization

[encoder]
model_name = "speaker_encoder.onnx"
input_tensor = "mel_input"    # (1, n_mels, time)
output_tensor = "embedding"   # (1, 256)
l2_normalize = true

[vits_integration]
input_tensor = "speaker_embedding"  # (1, 256)
zero_means_disabled = true          # 全ゼロ -> sid fallback
```

### 2.3 Python 実装

```python
# api.py に追加
class PiperPlus:
    def synthesize(self, text: str, *, reference_audio: str | np.ndarray | None = None,
                   speaker_id: int = 0, **kwargs) -> AudioResult:
        if reference_audio is not None:
            embedding = self._speaker_encoder.encode(reference_audio)
            return self._infer_with_embedding(text, embedding, **kwargs)
        return self._infer_with_sid(text, speaker_id, **kwargs)
```

### 2.4 Rust 実装

```rust
// engine.rs に追加
pub struct SynthesisParams {
    pub text: String,
    pub speaker_id: Option<i64>,
    pub speaker_embedding: Option<Vec<f32>>,  // NEW
    pub reference_audio: Option<AudioData>,    // NEW: 自動 encode
    // ...
}

impl PiperModel {
    pub fn encode_speaker(&self, audio: &AudioData) -> Result<Vec<f32>>;
    pub fn synthesize_with_embedding(&self, text: &str, embedding: &[f32]) -> Result<AudioData>;
}
```

### 2.5 C# 実装

```csharp
// Inference/ に追加
public class SpeakerEncoder : IDisposable
{
    public float[] Encode(ReadOnlySpan<float> audio, int sampleRate = 16000);
    public float[] EncodeFile(string wavPath);
}

// PiperPlusSynthesizer に追加
public AudioResult Synthesize(string text, SynthesisOptions options);

public class SynthesisOptions
{
    public int SpeakerId { get; set; } = 0;
    public float[]? SpeakerEmbedding { get; set; }  // NEW
    public string? ReferenceAudioPath { get; set; }  // NEW
}
```

### 2.6 Go 実装

```go
// piperplus/ に追加
type SpeakerEncoder struct { /* ... */ }

func (e *SpeakerEncoder) Encode(audio []float32, sampleRate int) ([]float32, error)
func (e *SpeakerEncoder) EncodeFile(wavPath string) ([]float32, error)

type SynthesisOptions struct {
    SpeakerID         int
    SpeakerEmbedding  []float32  // NEW
    ReferenceAudio    []float32  // NEW
}
```

### 2.7 WASM 実装

```javascript
// index.js に追加
class PiperPlus {
  async synthesize(text, options = {}) {
    if (options.referenceAudio) {
      const embedding = await this.encodeSpeaker(options.referenceAudio);
      return this._inferWithEmbedding(text, embedding, options);
    }
    return this._inferWithSid(text, options.speakerId ?? 0, options);
  }

  async encodeSpeaker(audioBuffer) {
    // AudioContext でリサンプリング -> メル変換 -> ONNX 推論
  }
}
```

### 2.8 CLI 統合

全ランタイムの CLI に以下のオプションを追加:

| オプション | 説明 |
|-----------|------|
| `--reference-audio <path>` | 参照音声 WAV ファイルパス |
| `--speaker-embedding <path>` | 事前抽出した embedding (npy/json) |
| `--encode-speaker <path>` | 音声 -> embedding の抽出のみ実行 |

## 3. エージェントチームの構成

| 役割 | 人数 | 担当内容 |
|------|------|---------|
| Python エンジニア | 1 | Python ランタイム + API 統合 |
| Rust エンジニア | 1 | Rust piper-core + CLI + Python バインディング |
| C# エンジニア | 1 | PiperPlus.Core + CLI |
| Go エンジニア | 1 | piperplus パッケージ + CLI |
| WASM エンジニア | 1 | ブラウザ向け実装 (AudioContext, WebGPU) |
| テスト統合担当 | 1 | 全ランタイムの横断テスト、embedding 一致性検証 |

## 4. テスト計画

### 提供範囲（スコープ）
- IN: 全5ランタイムでの Speaker Encoder 推論、音声前処理、Voice Cloning 推論パイプライン
- OUT: モデル学習 (M3-3)、Speaker Encoder の精度改善 (M3-1)

### ユニットテスト

各ランタイムで以下を実装:
- `test_mel_spectrogram`: 同一音声 -> 同一メルスペクトログラム (Python を基準に L2 < 1e-3)
- `test_speaker_encode`: 同一音声 -> 同一 embedding (Python を基準に L2 < 1e-3)
- `test_voice_cloning_inference`: 参照音声 + テキスト -> 音声出力 (WAV バイト長 > 0)
- `test_fallback_to_sid`: `speaker_embedding` なしで既存の `speaker_id` 動作
- `test_cli_reference_audio`: CLI `--reference-audio` オプションの動作

### E2Eテスト

- 5ランタイム x 3言語 (ja, en, zh) x 2参照話者 = 30パターンの Voice Cloning 推論
- 全ランタイムで同一参照音声 -> 同一 embedding (cosine similarity > 0.999)
- WASM: ブラウザ環境 (Chrome, Firefox) での動作確認

### クロスランタイム一致性テスト

```bash
# Python で embedding 抽出
uv run python -c "from piper_train.speaker_encoder import SpeakerEncoder; ..."

# Rust で embedding 抽出
cargo run -p piper-plus-cli -- --encode-speaker ref.wav --output embedding.json

# 比較
# cosine similarity > 0.999 を要求
```

## 5. 懸念事項とレビュー項目

### 懸念事項

1. **メルスペクトログラムの実装差異**: FFT/メル実装が言語ごとに異なるため、微小な数値差が累積する可能性。仕様を TOML で厳密に定義し、テスト音声での出力比較で検証。特に WASM (WebAudio API のリサンプリング精度) に注意
2. **Speaker Encoder モデルのダウンロード**: 各ランタイムで既存のモデル自動DL機構に Speaker Encoder を追加する必要がある。`ayousanz/piper-plus-speaker-encoder` からの DL パスを統一
3. **WASM のメモリ制約**: Speaker Encoder (~5-8MB) + VITS モデル (~75MB) をブラウザメモリに同時ロード。WebGPU 利用で緩和できるが、モバイルブラウザでは OOM のリスク
4. **全実装の同期リリース**: 5ランタイム全ての実装が揃わないとリリースできない。並行開発でボトルネックが発生しやすい

### レビューチェックリスト

- [ ] 全ランタイムのメルスペクトログラム出力が Python 基準で L2 < 1e-3
- [ ] 全ランタイムの embedding 出力が Python 基準で cosine similarity > 0.999
- [ ] 全ランタイムの CLI に `--reference-audio` オプションが追加されている
- [ ] `docs/spec/speaker-encoder-preprocess.toml` の仕様に全実装が準拠
- [ ] 既存テストの回帰がない
- [ ] WASM のブラウザテストが Chrome/Firefox で通過
- [ ] `PIPER_OFFLINE_MODE=1` で Speaker Encoder の自動DL が無効化される

## 6. 一から作り直すとしたら

**マルチランタイム同期開発の課題:**

5ランタイムへの同時展開は piper-plus の強みだが、Voice Cloning のような複雑な機能を全ランタイムに同時実装するのはリスクが高い。ゼロから設計するなら:

1. **Phase 1**: Python + Rust のみで Voice Cloning をリリース (v2.0.0-alpha)
2. **Phase 2**: C# / Go を追加 (v2.0.0-beta)
3. **Phase 3**: WASM を追加 (v2.0.0)

この段階的リリースにより、Phase 1 で API 設計を固め、Phase 2-3 での手戻りを最小化できる。

**メルスペクトログラム実装の統一について:**

最大の技術的課題は5言語でのメルスペクトログラム実装の一致。ゼロから設計するなら、メルスペクトログラム変換自体を ONNX モデルに含めてしまう (音声波形入力 -> embedding 出力) ことで、ランタイム側の前処理を完全に排除できる。ただし ONNX の FFT サポートは opset 17 からであり、全ランタイムの ONNX Runtime バージョンを確認する必要がある。

**Voice Cloning のアーキテクチャ選択の振り返り:**

ZSE-VITS (Speaker Encoder + VITS) は「既存 VITS に最小限の変更で Voice Cloning を追加する」アプローチとして適切。ただし、品質の上限は Speaker Encoder の性能に制約される。ゼロから設計するなら、VALL-E のような codec-based approach や CosyVoice の Flow Matching approach のほうが品質上限は高い。しかし、これらはモデルサイズが大きく (500MB+)、piper-plus の軽量性コンセプトと矛盾する。

M3a 全体として「軽量 Voice Cloning」という方向性は正しく、ECAPA-TDNN + VITS2 はその制約下での最適解に近い。

## 7. 後続タスクへの連絡事項

- **M3-8**: Voice Cloning の MOS ベンチマークを含める。各ランタイムでの推論速度も計測
- v2.0.0 の破壊的変更として `SynthesisParams` API の変更を Rust v1.14.0 で deprecation warning を出す (M2 で対応済みの前提)
- WASM パッケージ (`piper-plus`) のメジャーバージョンアップ (1.x -> 2.0) が必要
- C# パッケージ (`PiperPlus.Core`) のメジャーバージョンアップ (1.x -> 2.0) が必要
- Speaker Encoder モデルの自動DL URL を各ランタイムのモデルマネージャに登録: `ayousanz/piper-plus-speaker-encoder`
