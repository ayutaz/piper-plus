# [M3-2] SynthesizerTrn に `speaker_embedding` 入力パス追加

**マイルストーン**: M3a (v2.0.0)
**カテゴリ**: Voice Cloning
**工数**: 5d
**依存**: M3-GATE 通過
**ステータス**: 未着手

---

## 1. 目的とゴール

VITS の SynthesizerTrn モデルに、外部から speaker embedding を直接注入する入力パスを追加する。既存の `speaker_id` (整数 -> embedding lookup) に加え、Speaker Encoder が出力する連続ベクトルを受け取る `speaker_embedding` テンソルを ONNX グラフに追加する。既存モデルとの後方互換性を維持する。

**ゴール:**
- `SynthesizerTrn` の forward パスに `speaker_embedding: Optional[Tensor]` 引数を追加
- ONNX エクスポートで `speaker_embedding` を Optional 入力テンソルとして出力
- `speaker_embedding` が与えられた場合は `emb_g` lookup をスキップし、直接使用
- 既存の `speaker_id` ベースの推論が全く影響を受けないこと

## 2. 実装内容の詳細

### 2.1 models.py の変更

```python
# SynthesizerTrn.forward() / infer() に引数追加
def infer(self, x, x_lengths, sid=None, lid=None,
          noise_scale=0.667, length_scale=1.0, noise_w=0.8,
          prosody_features=None,
          speaker_embedding=None):  # NEW
    # ...
    if speaker_embedding is not None:
        g = speaker_embedding.unsqueeze(-1)  # (B, gin_channels, 1)
    elif sid is not None:
        g = self.emb_g(sid)  # 既存パス
    else:
        g = None
```

### 2.2 ONNX エクスポートの変更 (export_onnx.py)

ONNX グラフに以下の Optional 入力を追加:

| テンソル名 | 型 | 形状 | 必須 | 説明 |
|-----------|-----|------|------|------|
| `speaker_embedding` | float32 | (1, gin_channels) | No | 外部 Speaker Encoder の出力 |

既存入力テンソル (`input`, `input_lengths`, `scales`, `sid`, `lid`) は変更なし。`speaker_embedding` が提供されない場合は既存の `sid` -> `emb_g` lookup で動作する。

### 2.3 推論パス分岐のロジック

```
speaker_embedding が非ゼロ?
  ├─ Yes → g = speaker_embedding (Voice Cloning モード)
  └─ No
       └─ sid が有効?
            ├─ Yes → g = emb_g(sid) (既存の multi-speaker モード)
            └─ No → g = None (single-speaker モード)
```

ONNX では Optional 入力の扱いがランタイムによって異なるため、`speaker_embedding` は常に入力テンソルとして存在するが、全要素ゼロの場合は無視する方式を採用する。

### 2.4 次元マッチング

| ソース | 次元 | 一致 |
|--------|------|------|
| ECAPA-TDNN 出力 (M3-1) | 256 | gin_channels=256 と一致 |
| 既存 emb_g | gin_channels (256) | - |

M3-GATE で「条件付き Go」の場合は線形射影層を追加:
```python
self.speaker_proj = nn.Linear(encoder_dim, gin_channels)
```

### 2.5 後方互換性の保証

| シナリオ | 動作 | 互換性 |
|---------|------|--------|
| 既存 VITS ONNX (v1.x) + 新ランタイム | `speaker_embedding` 入力なし → `sid` 使用 | 完全互換 |
| 新 VITS ONNX (v2.0) + 旧ランタイム | `speaker_embedding` 無視 → `sid` 使用 | 完全互換 |
| 新 VITS ONNX + 新ランタイム + speaker_embedding | Voice Cloning モード | 新機能 |

### 2.6 影響範囲

| ファイル | 変更内容 |
|---------|---------|
| `src/python/piper_train/vits/models.py` | `infer()` に `speaker_embedding` 引数追加 |
| `src/python/piper_train/export_onnx.py` | ONNX 入力テンソルに `speaker_embedding` 追加 |
| `src/python/piper_train/infer_onnx.py` | `--speaker-embedding` オプション追加 |
| `src/python/piper_train/vits/lightning.py` | training step での speaker_embedding 対応 (学習時) |

## 3. エージェントチームの構成

| 役割 | 人数 | 担当内容 |
|------|------|---------|
| VITS モデルエンジニア | 1 | models.py の forward/infer パス変更、次元マッチング |
| ONNX エンジニア | 1 | export_onnx.py の Optional 入力テンソル対応、後方互換テスト |
| テスト担当 | 1 | 既存モデルの回帰テスト、新パスの動作検証 |

## 4. テスト計画

### 提供範囲（スコープ）
- IN: `SynthesizerTrn` への `speaker_embedding` 入力パス追加、ONNX エクスポート対応、後方互換性
- OUT: Speaker Encoder との結合テスト (M3-3)、各ランタイム対応 (M3-4)

### ユニットテスト

- `test_infer_with_sid`: 既存の `speaker_id` パスが変更なく動作すること (回帰テスト)
- `test_infer_with_speaker_embedding`: `speaker_embedding` テンソルで推論が動作すること
- `test_infer_speaker_embedding_priority`: `sid` と `speaker_embedding` 両方指定時に `speaker_embedding` が優先されること
- `test_infer_zero_embedding_fallback`: 全ゼロ `speaker_embedding` で `sid` にフォールバックすること
- `test_speaker_proj_layer`: 次元不一致時の線形射影層が正しく動作すること (条件付き Go の場合)
- `test_export_onnx_with_speaker_embedding`: ONNX グラフに `speaker_embedding` 入力が含まれること
- `test_onnx_backward_compat`: v1.x ONNX モデルが新ランタイムで動作すること

### E2Eテスト

- テストモデル (`test/models/multilingual-test-medium.onnx`) を再エクスポートし、`speaker_embedding` 入力ありで推論
- ランダム embedding で音声生成 -> WAV バイト長 > 0 の検証
- 既存テスト全件の回帰テスト実行

## 5. 懸念事項とレビュー項目

### 懸念事項

1. **ONNX Optional 入力の互換性**: ONNX Runtime のバージョンによって Optional 入力の扱いが異なる。全ゼロテンソル方式は最も互換性が高いが、ゼロベクトルが有効な embedding と区別できない問題がある。`speaker_embedding_mask` (0/1) の追加も検討
2. **既存モデルの非互換リスク**: ONNX グラフに新しい入力が追加されると、入力名のハードコードに依存している旧ランタイムがクラッシュする可能性。ONNX の Optional 入力 (opset 16+) を使えばグラフレベルで互換性を担保できるが、WASM の ONNX Runtime バージョンを確認する必要がある
3. **学習パイプラインへの影響**: `lightning.py` の training step で `speaker_embedding` をどう扱うか。学習時は `sid` -> `emb_g` を使い、推論時のみ `speaker_embedding` を使う設計が最もシンプル

### レビューチェックリスト

- [ ] `models.py` の変更が既存の全テストをパスすること
- [ ] ONNX グラフの入出力仕様が `docs/spec/` にドキュメント化されている
- [ ] 全ゼロ embedding のフォールバック動作が明確にテストされている
- [ ] `export_onnx.py` の `--unify-emb-lang` との相互作用が検証されている
- [ ] `freeze-dp` との相互作用が検証されている (Voice Cloning + DP 凍結)

## 6. 一から作り直すとしたら

**VITS の speaker conditioning アーキテクチャの再設計:**

現在の VITS は `emb_g(sid)` で離散的な speaker embedding を lookup し、各モジュール (encoder, decoder, flow, dp) の条件付けに使用している。Voice Cloning で外部 embedding を注入する場合、この lookup を bypass するだけなので変更は最小限。

しかし、ゼロから設計するなら以下を検討すべき:

1. **Adaptive Layer Norm (AdaLN)**: speaker embedding を LayerNorm のスケール/バイアスとして注入 (StyleTTS 方式)。`emb_g` の単純な concat よりも表現力が高い
2. **Cross-Attention**: speaker embedding を key/value として各層で cross-attention (Tortoise-TTS 方式)。最も柔軟だが計算コスト大
3. **FiLM (Feature-wise Linear Modulation)**: 各層で `gamma * x + beta` の変調 (StyleSpeech 方式)。AdaLN より軽量

VITS の既存設計 (concat + 1x1 conv) は最もシンプルで、piper-plus の軽量性コンセプトに合致している。AdaLN への移行は M3-5 (VITS2) と合わせて検討すべきだったが、破壊的変更が大きすぎるため v3.0.0 以降のロードマップとする判断は妥当。

**VITS2 移行とのタイミングについて:** M3-2 (speaker_embedding) を VITS1 で実装してから M3-5 (VITS2) に移行するため、M3-5 で models.py を再度変更する必要がある。ゼロから設計するなら M3-5 を先に実施し、VITS2 の models.py に直接 speaker_embedding を追加すべき。ただし M3-5 は M3-GATE に依存しないため並行作業可能であり、現在の順序でも手戻りは models.py の差分マージ程度で済む。

## 7. 後続タスクへの連絡事項

- **M3-3**: 学習時に `speaker_embedding` を使う場合のデータパイプライン設計が必要。事前に全話者の embedding を Speaker Encoder で抽出してキャッシュする方式を推奨
- **M3-4**: 各ランタイムの ONNX 入力テンソル構築で `speaker_embedding` を追加。テンソル名は `"speaker_embedding"`, 形状は `(1, gin_channels)`, dtype は `float32`。全ゼロの場合は `sid` にフォールバック
- **M3-5**: VITS2 への移行時に `speaker_embedding` パスを引き継ぐこと。`models.py` のマージ時に注意
- ONNX 入出力仕様の変更を `docs/spec/onnx-io-spec.toml` (新規) に記録し、全ランタイムで参照
