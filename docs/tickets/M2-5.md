# M2-5: `test_prosody_extraction.py` — `validate_dataset_prosody()` 自己テストを本番抽出or削除

**マイルストーン**: M2 — テストのみの修正
**ステータス**: [ ] 未着手
**ブロッカー**: M0 完了後
**後続タスク**: なし

---

## 1. 目的とゴール

`test_prosody_extraction.py` L550-574 にて、テスト内部にローカル関数 `validate_dataset_prosody()` が定義され、その関数自体のテストが同一テストケース内で行われている。これは「テストコード内に本番相当のロジックを書き、そのロジック自体をテストする」自己参照パターンであり、テストとしての価値が曖昧である。

**現状のコード** (`test_prosody_extraction.py` L550-574):

```python
def validate_dataset_prosody(dataset_lines):
    """Validate that all prosody_features match phoneme_ids lengths."""
    errors = []
    for i, line in enumerate(dataset_lines):
        data = json.loads(line) if isinstance(line, str) else line
        pids = data.get("phoneme_ids", [])
        pf = data.get("prosody_features", [])
        if pf and len(pids) != len(pf):
            errors.append({
                "line": i,
                "phoneme_ids_len": len(pids),
                "prosody_features_len": len(pf),
                "text": data.get("text", "")[:50]
            })
    return errors

# Valid data should have no errors
valid_errors = validate_dataset_prosody([json.dumps(d) for d in valid_data])
assert len(valid_errors) == 0, f"Valid data should have no errors: {valid_errors}"

# Invalid data should be detected
invalid_errors = validate_dataset_prosody([json.dumps(d) for d in invalid_data])
assert len(invalid_errors) == 1, f"Invalid data should have 1 error: {invalid_errors}"
assert invalid_errors[0]["phoneme_ids_len"] == 5
assert invalid_errors[0]["prosody_features_len"] == 3
```

**問題点**:
1. `validate_dataset_prosody()` はテスト内で定義・テストされているが、本番コードにはこの関数が存在しない (grepで確認済み: テストファイル以外にヒットなし)
2. テスト内で定義した関数をテストしても、本番コードの品質保証にならない
3. もしこのバリデーションロジックが有用なら、本番コード (`piper_train.tools` 等) に抽出すべき

**完了の定義** (以下のいずれか):
- **Option A (本番抽出)**: `validate_dataset_prosody()` を本番コード (例: `piper_train/tools/dataset_utils.py`) に移動し、テストはその本番関数を import して検証する
- **Option B (削除)**: テスト内ローカル関数とその自己テスト部分 (L550-574) を削除し、テストケース `test_validate_dataset_prosody_lengths` 自体をL436-511 の `test_prosody_length_matches_phoneme_ids_various_texts` に集約する (既に同等の検証が行われている)

## 2. 実装の詳細

### 2.1 推奨: Option B (削除)

`test_prosody_length_matches_phoneme_ids_various_texts` (L436-511) が既に以下を検証している:
- 各テキストに対して `phonemize_japanese_with_prosody()` を呼び出し
- `len(tokens) == len(prosody_info)` を全テキストで assert

L550-574 の自己テストは、手動で作った valid/invalid データに対してローカル関数が正しく動くかを検証しているだけで、本番のprosody抽出パイプラインの品質保証には寄与していない。

### 2.2 変更ファイル

| ファイル | 変更内容 |
|----------|---------|
| `src/python/tests/test_prosody_extraction.py` | L550-574 の `validate_dataset_prosody()` 定義とその呼び出し・assert を削除。テストケース `test_validate_dataset_prosody_lengths` の前半 (L514-548: valid/invalid データの作成) は残してもよいが、自己テスト部分は削除 |

### 2.3 修正後のコード

`test_validate_dataset_prosody_lengths` を簡素化。直接 `phoneme_ids` と `prosody_features` の長さの一致/不一致を assert する形に書き換える:

```python
@pytest.mark.unit
def test_validate_dataset_prosody_lengths(self):
    """Test that prosody_features length mismatches are detectable in dataset entries."""
    import json

    # Valid: phoneme_ids と prosody_features の長さが一致
    valid_entry = {
        "phoneme_ids": [1, 2, 3, 4, 5],
        "prosody_features": [
            {"a1": 0, "a2": 0, "a3": 0},
            {"a1": -1, "a2": 1, "a3": 3},
            {"a1": 0, "a2": 2, "a3": 3},
            {"a1": 1, "a2": 3, "a3": 3},
            {"a1": 0, "a2": 0, "a3": 0},
        ],
    }
    assert len(valid_entry["phoneme_ids"]) == len(valid_entry["prosody_features"])

    # Invalid: 長さ不一致を検出できることを確認
    invalid_entry = {
        "phoneme_ids": [1, 2, 3, 4, 5],  # 5 items
        "prosody_features": [
            {"a1": 0, "a2": 0, "a3": 0},
            {"a1": -1, "a2": 1, "a3": 3},
            {"a1": 0, "a2": 2, "a3": 3},
        ],  # 3 items — MISMATCH
    }
    assert len(invalid_entry["phoneme_ids"]) != len(invalid_entry["prosody_features"])
```

## 3. エージェントチーム構成

| 役割 | 人数 | 担当内容 |
|------|------|---------|
| 実装者 | 1 | テストコード修正 |
| レビュアー | 1 | 削除漏れ・テストカバレッジ確認 |

## 4. 提供範囲とテスト

### 提供範囲

| ファイル | 変更種別 | 変更行数 (概算) |
|----------|---------|----------------|
| `src/python/tests/test_prosody_extraction.py` | ローカル関数削除 + テスト簡素化 | +10 / -25 |

### ユニットテスト

- `test_validate_dataset_prosody_lengths` が PASS する
- `test_prosody_length_matches_phoneme_ids_various_texts` が引き続き PASS する (本テストが本番パイプラインの長さ一致を真にテストしている)

### E2Eテスト

- `pytest src/python/tests/test_prosody_extraction.py -v` — 全テスト PASS
- CI が影響を受けないこと

## 5. 懸念事項とレビュー項目

1. **Option A (本番抽出) の検討**: もし `prepare_multilingual_dataset.py` や `add_prosody_features.py` でデータセットバリデーション機能が実際に必要な場合は、Option A を選択して `validate_dataset_prosody()` を本番ユーティリティとして公開する方がよい。ただし現時点では本番コードにそのようなバリデーション関数は存在しないため、YAGNI原則に従い Option B を推奨する。

2. **テストカバレッジの低下**: L550-574 を削除しても、L436-511 の `test_prosody_length_matches_phoneme_ids_various_texts` が 11 種類のテキストに対して実際の `phonemize_japanese_with_prosody()` の長さ一致を検証しているため、実質的なカバレッジ低下はない。

## 6. 一から作り直すとしたら

データセットバリデーションは本番ツール (`prepare_multilingual_dataset.py`) の最終ステップに組み込むべき。テスト側では「本番のバリデーション関数が不正データを正しく検出するか」をテストする構成が望ましい。自己テストパターンは設計段階で避ける。

## 7. 後続タスクへの連絡事項

- 特になし。削除のみの変更であり、他タスクへの影響はない。
