# Core dependencies
onnxruntime>=1.11.0
numpy>=1.24.0,<3.0

# Japanese phonemization (cross-platform, no espeak-ng dependency)
pyopenjtalk-plus>=0.4

# English phonemization
g2p-en>=2.1.0

# Chinese phonemization
pypinyin>=0.50

# Note: piper-phonemize (espeak-ng based) is optional.
# Install with: pip install piper-tts-plus[espeak]
# Required only for PhonemeType.ESPEAK models (not needed for multilingual/JA models).
