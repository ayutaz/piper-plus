diff --git a/.github/workflows/dev-daily-release.yml b/.github/workflows/dev-daily-release.yml
index 1234567..2345678 100644
--- a/.github/workflows/dev-daily-release.yml
+++ b/.github/workflows/dev-daily-release.yml
@@ -736,19 +736,37 @@ jobs:
             ls -la piper/lib/ || true
           fi
           
-          # Download test model (skip for now - model URL needs to be verified)
-          echo "Skipping Japanese model download for now..."
-          # curl -L -o test/models/ja_JP-test-medium.onnx \
-          #   "https://github.com/rhasspy/piper/releases/download/v1.2.0/voice-ja_JP-test-medium.onnx" || \
-          #   echo "Warning: Failed to download model, will try with built-in test"
+          # Check if espeak-ng data is available first
+          if [ -d "piper/espeak-ng-data" ]; then
+            echo "Found espeak-ng data directory"
+            export ESPEAK_DATA_PATH=$PWD/piper/espeak-ng-data
+          elif [ -d "piper/share/espeak-ng-data" ]; then
+            echo "Found espeak-ng data in share directory"
+            export ESPEAK_DATA_PATH=$PWD/piper/share/espeak-ng-data
+          else
+            echo "Warning: espeak-ng data not found"
+          fi
           
           # Run TTS with help first to verify binary works
+          echo "Testing binary with --help..."
           "${{ matrix.binary_path }}" --help || echo "Warning: Help command failed"
           
-          # Run TTS
-          if [ -f "test/models/ja_JP-test-medium.onnx" ]; then
-            cat test_input.txt | "${{ matrix.binary_path }}" --model test/models/ja_JP-test-medium.onnx --output_file test_output.wav
+          # Download a small test model
+          echo "Downloading test model..."
+          mkdir -p test/models
+          # Use a smaller English model for basic functionality test
+          curl -L -o test/models/en_US-lessac-medium.onnx \
+            "https://huggingface.co/rhasspy/piper-voices/resolve/main/en/en_US/lessac/medium/en_US-lessac-medium.onnx" || {
+            echo "Warning: Failed to download model"
+          }
+          
+          if [ -f "test/models/en_US-lessac-medium.onnx" ]; then
+            curl -L -o test/models/en_US-lessac-medium.onnx.json \
+              "https://huggingface.co/rhasspy/piper-voices/resolve/main/en/en_US/lessac/medium/en_US-lessac-medium.onnx.json"
+            echo "Testing with English model..."
+            echo "Hello, this is a test." | "${{ matrix.binary_path }}" \
+              --model test/models/en_US-lessac-medium.onnx --output_file test_output.wav
           else
             # Fallback: just test that the binary runs
             echo "Testing binary functionality..."
@@ -756,13 +774,12 @@ jobs:
             "${{ matrix.binary_path }}" --version || echo "Version check failed"
             
             # Check if espeak-ng data is available
-            if [ -d "piper/espeak-ng-data" ]; then
-              echo "Found espeak-ng data directory"
-              export ESPEAK_DATA_PATH=$PWD/piper/espeak-ng-data
-            fi
+            echo "ESPEAK_DATA_PATH=$ESPEAK_DATA_PATH"
             
-            # Try simple TTS without model (will fail but shows binary works)
-            echo "Testing" | "${{ matrix.binary_path }}" --output_file test_output.wav 2>&1 | grep -E "(model|help|usage)" || true
+            # List directory contents for debugging
+            echo "Directory contents:"
+            ls -la piper/ || true
+            ls -la piper/share/ || true
           fi
           
           # Check if output was created