--- a/lib/HTS_audio.c
+++ b/lib/HTS_audio.c
@@ -69,6 +69,19 @@ HTS_AUDIO_C_START;
 
 #ifdef AUDIO_PLAY_WIN32
 
+/* Define architecture macros required by Windows SDK */
+#if !defined(_AMD64_) && !defined(_X86_) && !defined(_ARM64_)
+  #if defined(_M_AMD64) || defined(_M_X64)
+    #define _AMD64_ 1
+  #elif defined(_M_IX86)
+    #define _X86_ 1
+  #elif defined(_M_ARM64)
+    #define _ARM64_ 1
+  #endif
+#endif
+
+#define WIN32_LEAN_AND_MEAN
+#define NOMINMAX
 #include <windows.h>
 #include <mmsystem.h>
 #define AUDIO_WAIT_BUFF_MS 10   /* wait time (0.01 sec) */