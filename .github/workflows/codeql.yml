name: CodeQL

# CodeQL static analysis. Runs on PR / push to dev / weekly schedule.
# Rust は GitHub CodeQL 未対応のため除外。
# Swift / Kotlin は `build-mode: none` (CodeQL CLI 2.18.4+ の no-build mode)
# を使用。理由:
#   - Swift: Package.swift が未公開 release tag (v1.13.0 / v1.14.0) の
#     binary target を参照するため、`swift build` は dev branch で 404 失敗。
#     これは tag-time 確定方式 (Package.swift header 参照) の設計。
#   - Kotlin: AAR build は事前ビルド済み libpiper_plus.so が jniLibs/<abi>/
#     に staged されている前提で、CodeQL autobuild 環境では存在しない。
# no-build mode は syntactic 解析のみだが、SAST の主要 query は build
# artifact 不要で動作するため十分。tag-time の release CI で binary を
# build する別 job を追加すれば semantic 解析に格上げ可能 (将来課題)。

on:
  pull_request:
    branches: [ dev ]
  push:
    branches: [ dev ]
  schedule:
    - cron: '0 8 * * 1'  # Monday 08:00 UTC

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref }}
  cancel-in-progress: true

permissions:
  actions: read
  contents: read
  security-events: write

jobs:
  filter:
    runs-on: ubuntu-latest
    outputs:
      python: ${{ steps.changes.outputs.python }}
      javascript-typescript: ${{ steps.changes.outputs.javascript-typescript }}
      go: ${{ steps.changes.outputs.go }}
      csharp: ${{ steps.changes.outputs.csharp }}
      cpp: ${{ steps.changes.outputs.cpp }}
      swift: ${{ steps.changes.outputs.swift }}
      kotlin: ${{ steps.changes.outputs.kotlin }}
    steps:
      - uses: actions/checkout@v6
      - id: changes
        # On schedule we run all languages; on PR we filter via paths.
        if: github.event_name == 'pull_request' || github.event_name == 'push'
        uses: dorny/paths-filter@v3
        with:
          filters: |
            python:
              - 'src/python/**'
              - 'src/python_run/**'
              - 'docker/python-inference/**'
              - 'docker/webui/**'
              - 'docker/wyoming/**'
              - 'scripts/**'
              - '.github/workflows/codeql.yml'
            javascript-typescript:
              - 'src/wasm/**'
              - '.github/workflows/codeql.yml'
            go:
              - 'src/go/**'
              - '.github/workflows/codeql.yml'
            csharp:
              - 'src/csharp/**'
              - '.github/workflows/codeql.yml'
            cpp:
              - 'src/cpp/**'
              - 'cmake/**'
              - 'CMakeLists.txt'
              - '.github/workflows/codeql.yml'
            swift:
              - 'Sources/**'
              - 'Tests/**'
              - 'Package.swift'
              - 'examples/swift-g2p/**'
              - '.github/workflows/codeql.yml'
            kotlin:
              - 'android/**'
              - '.github/workflows/codeql.yml'

  analyze:
    needs: filter
    name: Analyze (${{ matrix.language }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 60
    permissions:
      actions: read
      contents: read
      security-events: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - language: python
            os: ubuntu-24.04
            build-mode: none
          - language: javascript-typescript
            os: ubuntu-24.04
            build-mode: none
          - language: go
            os: ubuntu-24.04
            build-mode: autobuild
          - language: csharp
            os: ubuntu-24.04
            build-mode: manual
          - language: cpp
            os: ubuntu-24.04
            build-mode: manual
          - language: swift
            os: macos-15
            # no-build mode: SPM binary target の release artifact 解決を
            # 回避 (Package.swift header の tag-time 確定方式設計のため、
            # dev branch では 404 が正常)。
            build-mode: none
          - language: kotlin
            # CodeQL の Kotlin 解析は java-kotlin として扱う。matrix の表示名
            # は kotlin だが init 内部で java-kotlin にマップする (下の step
            # で `languages: java-kotlin` を渡す)。
            # no-build mode: AGP/Gradle の jniLibs/<abi>/libpiper_plus.so
            # 依存を回避 (CodeQL 環境には事前ビルド済み .so が存在しない)。
            os: ubuntu-24.04
            build-mode: none

    steps:
      - name: Skip if no relevant changes
        id: should-skip
        run: |
          if [ "${{ github.event_name }}" = "schedule" ]; then
            echo "skip=false" >> $GITHUB_OUTPUT
          elif [ "${{ matrix.language }}" = "python" ] && [ "${{ needs.filter.outputs.python }}" = "true" ]; then
            echo "skip=false" >> $GITHUB_OUTPUT
          elif [ "${{ matrix.language }}" = "javascript-typescript" ] && [ "${{ needs.filter.outputs.javascript-typescript }}" = "true" ]; then
            echo "skip=false" >> $GITHUB_OUTPUT
          elif [ "${{ matrix.language }}" = "go" ] && [ "${{ needs.filter.outputs.go }}" = "true" ]; then
            echo "skip=false" >> $GITHUB_OUTPUT
          elif [ "${{ matrix.language }}" = "csharp" ] && [ "${{ needs.filter.outputs.csharp }}" = "true" ]; then
            echo "skip=false" >> $GITHUB_OUTPUT
          elif [ "${{ matrix.language }}" = "cpp" ] && [ "${{ needs.filter.outputs.cpp }}" = "true" ]; then
            echo "skip=false" >> $GITHUB_OUTPUT
          elif [ "${{ matrix.language }}" = "swift" ] && [ "${{ needs.filter.outputs.swift }}" = "true" ]; then
            echo "skip=false" >> $GITHUB_OUTPUT
          elif [ "${{ matrix.language }}" = "kotlin" ] && [ "${{ needs.filter.outputs.kotlin }}" = "true" ]; then
            echo "skip=false" >> $GITHUB_OUTPUT
          else
            echo "skip=true" >> $GITHUB_OUTPUT
          fi

      - uses: actions/checkout@v6
        if: steps.should-skip.outputs.skip != 'true'

      - name: Setup Go
        if: steps.should-skip.outputs.skip != 'true' && matrix.language == 'go'
        uses: actions/setup-go@v5
        with:
          go-version: '1.26'
          cache-dependency-path: src/go/go.sum

      - name: Setup .NET
        if: steps.should-skip.outputs.skip != 'true' && matrix.language == 'csharp'
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 10.0.x

      - name: Setup Python
        if: steps.should-skip.outputs.skip != 'true' && matrix.language == 'python'
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Setup Node.js
        if: steps.should-skip.outputs.skip != 'true' && matrix.language == 'javascript-typescript'
        uses: actions/setup-node@v6
        with:
          node-version: '24.x'

      - name: Setup C++ build deps
        if: steps.should-skip.outputs.skip != 'true' && matrix.language == 'cpp'
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake build-essential

      - name: Initialize CodeQL
        if: steps.should-skip.outputs.skip != 'true'
        uses: github/codeql-action/init@v3
        with:
          # CodeQL は Kotlin を `java-kotlin` 言語として識別する。matrix の
          # 表示用 `kotlin` を init 渡し用にここでマップする。
          languages: ${{ matrix.language == 'kotlin' && 'java-kotlin' || matrix.language }}
          build-mode: ${{ matrix.build-mode }}
          queries: security-and-quality
          # paths-ignore (test/demo/dist exclusions): see config-file rationale.
          # Centralizes false-positive suppressions in one location instead of
          # duplicating into per-job conditionals.
          config-file: ./.github/codeql/codeql-config.yml

      - name: Build (autobuild for Go)
        if: steps.should-skip.outputs.skip != 'true' && matrix.build-mode == 'autobuild'
        uses: github/codeql-action/autobuild@v3

      - name: Manual build (C#)
        if: steps.should-skip.outputs.skip != 'true' && matrix.language == 'csharp'
        run: dotnet build src/csharp/PiperPlus.sln -c Release

      - name: Manual build (C++)
        if: steps.should-skip.outputs.skip != 'true' && matrix.language == 'cpp'
        run: |
          cmake -S . -B build -DCMAKE_BUILD_TYPE=Release -DBUILD_TESTING=OFF
          cmake --build build --parallel 2

      - name: Perform CodeQL Analysis
        if: steps.should-skip.outputs.skip != 'true'
        uses: github/codeql-action/analyze@v3
        with:
          category: "/language:${{ matrix.language == 'kotlin' && 'java-kotlin' || matrix.language }}"
