name: markdownlint

on:
  pull_request:
    paths:
      - '**/*.md'
      - '**/*.markdown'
      - '.markdownlint.yaml'
  push:
    branches: [dev, main]
    paths:
      - '**/*.md'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref }}
  cancel-in-progress: true

jobs:
  markdownlint:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6

      - uses: DavidAnson/markdownlint-cli2-action@v17
        continue-on-error: true  # baseline 確立まで warn-only (Wave 後段で strict 化)
        with:
          globs: |
            **/*.md
            !node_modules
            !.git
            !**/CHANGELOG.md
            !test/models/**
            !**/build/**
            !**/dist/**
            !**/target/**
          config: .markdownlint.yaml
